import React, { useRef } from 'react';
import { Engine } from 'tform-core';
export default function(){
    const engine = useRef(new Engine())
    engine.current.registerPlugins
    return <div>dasfasfsaf</div>
}
